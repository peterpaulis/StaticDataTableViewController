Pod::Spec.new do |s|
  s.name         = 'StaticDataTableViewController'
  s.version      = '2.0.2'
  s.summary      = 'Enabling animated hide/show of static cells for UITableView'
  s.author = {
    'Peter Paulis' => 'peter@min60.com'
  }
  s.source = {
    :git => 'https://github.com/mhaddl/StaticDataTableViewController',
    :tag => '2.0.2'
  }
  s.source_files = '*.{h,m}'
end